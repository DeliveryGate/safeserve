{
  "name": "safeserve-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd safeserve/web && npx prisma db push --schema=../prisma/schema.prisma --skip-generate && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
